# AI 向けの作業規約

このリポジトリは **仕様駆動開発（SDD）+ 自律ループ**で開発する。

## 最初に読むもの

1. `docs/loop/LOOP.md` — 定期実行時の手順（**毎晩のループはこの通りに動く**）
2. `docs/specs/02-requirements.md` — 何が実装済みで何が未実装か
3. `docs/specs/03-design.md` — なぜこの構造なのか
4. `docs/feedback/INBOX.md` — 人間からの未処理の指摘（**最優先**）
5. `docs/feedback/QUESTIONS.md` — AI からの質問と、その回答（**最優先**）

## 絶対のルール

| ルール | 理由 |
|---|---|
| **仕様を先に直す。コードが仕様と食い違ったら仕様が正** | 仕様がレビュー対象だから |
| **仕様が曖昧なら実装せず質問する** | 推測で作ると手戻りが大きい |
| **テストと lint が緑でなければコミットしない** | 壊れた状態を残さない |
| **コミット前に AI レビューを必ず通す**（LOOP.md ⑦） | 人間の承認を待たず `main` に入るため |
| **`docs/feedback/` の 2 本の台帳は他のどのタスクより優先** | 人間を待たせる時間が最も高くつく |
| **未回答の質問に依存するタスクは着手しない。同じ質問を聞き直さない** | 推測実装と催促の両方を避ける |
| **ループの push 先は `main`。PR は作らない** | 2026-08-15 に本人が許可（レビューは事後） |

## 書き方

### 仕様書（`docs/specs/`）
- **非エンジニアが 10 分で読める**ことを最優先にする
- 表で書けるものは表にする。1 文は 60 字程度まで
- **できないことを隠さない。** 未対応ほど目立つ位置に書く
- 出力例は必ず**実際のテストで検証済みの値**を貼る（嘘の例を書かない）

### コード（`src/`）
- コメント・docstring は日本語。「何を」ではなく**「なぜ」**を書く
- コア機能は標準ライブラリのみ。依存追加は仕様（03-design.md）に理由を書いてから
- 例外は `errors.py` の型を使う。利用者にスタックトレースを見せない
- 型注釈を付ける。`from __future__ import annotations` を使う

### テスト（`tests/`）
- 外部ファイルに依存しない（`tests/fixtures.py` が最小の OOXML を作る）
- 出力は**完全一致**で比較する（出力の安定性が要件 NFR-01）
- 1 テスト 1 事実。テスト名だけで壊れた仕様が分かるようにする
- テスト関数名は要件表（02-requirements.md）から参照されるので、安易に改名しない

## 開発コマンド

```bash
.venv/bin/pytest          # テスト
.venv/bin/ruff check .    # lint
.venv/bin/ruff format .   # 整形
```

venv がなければ `uv venv .venv && uv pip install --python .venv/bin/python -e ".[dev,pdf]"`。

## コミット

- 1 ループ 1 コミット。メッセージは日本語で「何を・なぜ」
- 仕様の変更とコードの変更は同じコミットに含める（対応関係を追えるように）
