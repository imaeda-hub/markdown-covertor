# ここに資料を置いてください

変換を試したい資料（`.docx` / `.xlsx` / `.pptx` / `.pdf`）をこのフォルダに置いて
push すると、次のループが変換して `../passing/` か `../failing/` に振り分けます。

- ファイル名は自由（日本語可）
- **公開して困る資料は置かないでください**（リポジトリの履歴に残ります）
- 目安は 1 ファイル 5MB 程度まで

判定基準と、その後の扱いは [../README.md](../README.md) を参照してください。
