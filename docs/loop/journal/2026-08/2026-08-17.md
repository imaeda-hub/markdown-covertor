# 2026-08-17 のループ

## 調査メモ
markitdown は v0.1.7、Docling は v2.117.0 が最新（両方とも 7 月末リリース）で、
pyproject.toml の下限（`markitdown[...]>=0.1.7`）は既に最新に追随できている。
GFM 仕様は 2019 年から凍結されたまま。方針を変える発見はなし。

## 取り込んだフィードバック
- INBOX.md の Docling 乗り換え提案（2026-08-16 起票）は未承認のまま。今回も着手せず据え置き
- QUESTIONS.md の未回答質問: なし（すべて決定の記録へ移動済み）

## やったこと
| タスク | 内容 | 変更ファイル |
|---|---|---|
| T-28 | Excel の数値が同じ列内で桁揃えされる問題（`0.6`→`0.60`、`2000`→`2000.0`）を修正。表セルの末尾の `0` を機械的に削るだけで直せた（値自体は変わっていないため） | `src/mdconv/postprocess.py`, `src/mdconv/api.py`, `tests/test_postprocess.py`, `tests/test_conversion.py` |
| T-23 | `資料.docx` と `資料.xlsx` のように同名・拡張子違いのファイルを一括変換すると出力先が衝突し、片方が消えていた問題を修正。衝突する組だけ拡張子を残した名前（`資料.docx.md`）にする | `src/mdconv/cli.py`, `tests/test_cli.py` |

## レビュー結果
`/code-review` スキルで独立レビューを実行（サブエージェントへの委譲ではなく単一パス、effort: medium）。

- **[修正]** `fix_number_padding` を全形式に無条件適用していた。Word の表は pandas を経由せずセルの文字列をそのまま出すため、利用者が意図して書いた `100.00` のような表記まで壊す実例を再現された。xlsx にのみ適用するよう `api.py` を修正。合わせて PowerPoint のグラフ表に残る同じ症状は T-35 として起票（別原因のため今回は直さない）
- **[修正]** `_destinations` の衝突回避フォールバックに到達しない `or "0"` が残っていた（正規表現の形から空文字にはなり得ない）。削除
- **[修正]** `03-design.md` の見出し「補完している 4 つのこと」が今回の追加で 7 節になり数字と食い違っていた。見出しから数を削った
- **[未修正・起票]** `_destinations` のフォールバックは「同名・拡張子違い」の衝突しか解決せず、`--recursive` で別フォルダの「同名・同形式」ファイル（`a/報告.docx` と `b/報告.docx`）は今回も衝突したまま。T-23 の元々のスコープ外だったため T-34 として別タスクに起票
- **[未修正]** `_destination()` の `multiple` 引数が実質常に `True` で呼ばれ、パラメータとして形骸化している（可読性の軽微な指摘）。影響が小さいためスキップ

指摘はすべて反映済み。反映後に `pytest` / `ruff check` / `ruff format --check` を再実行し、全緑を確認した。

## リポジトリの整備
**週次点検（月曜）を実施。**

- フォルダ構成・入口の可読性: `docs/README.md` / `README.md` を再読。今回の変更で古くなった記述はなし
- 使われていないファイル: `src/mdconv/*.py` を総当たりで相互参照チェック。すべて他ファイルから参照されており、死んだモジュールなし
- ドキュメントの重複: 目立った重複は見つからず
- 肥大したファイル: `src/mdconv/postprocess.py` が 357 行で 300 行の目安を超えている。今回の変更で 19 行増えて閾値超過が悪化した。分割は構成変更なので今回は行わず、`04-tasks.md` に検討タスクとして残す（T-36）

`tests/corpus/inbox/` は空。`tests/corpus/failing/` の 2 件（罫線なし表・スキャン PDF）は今回も再変換して症状が変わっていないことを確認した（T-05 / T-22 は据え置き）。

## 👀 見てほしいところ
- `src/mdconv/postprocess.py:fix_number_padding` の桁揃え除去ロジック（`api.py` で xlsx だけに絞った判断が妥当か）
- `src/mdconv/cli.py:_destinations` の衝突回避（拡張子を残す方式で十分か、T-34 の同名・同形式ケースまで今回のループで直すべきだったか）

## ❓ 判断を仰ぎたいこと
- 新規の質問なし。INBOX.md の Docling 乗り換え提案（未承認）のみ再掲: 承認が出るまで着手しない

## 積み残し
- T-24（PowerPoint / Excel の画像取り出し）: 調査した結果、markitdown が画像を **XML の出現順ではなく画面上の位置（top/left）順**に出すことが判明。単純な出現順マッチングでは画像を取り違える恐れがあり、1 回のループでは安全に実装しきれないと判断して見送った。次回着手する場合はこの並べ替えルールへの対応が前提になる
- T-35（PowerPoint のグラフの数値の桁揃え）: 今回新規に起票。T-28 と症状は同じだが、表の由来（グラフ API 経由）が違うため同じ直し方が使えない
- T-34（`--recursive` の同名・同形式ファイルの出力衝突）: レビューで発覚し新規に起票
- T-36（`postprocess.py` の分割検討）: 週次点検で発見、新規に起票済み。分割作業自体は次回以降

## 状態
テスト: 118 件すべて成功（1 件 skip）／ lint: 問題なし／ format: 問題なし
