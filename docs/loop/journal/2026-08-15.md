# 2026-08-15 のループ（初回：立ち上げ）

## 調査メモ
- PDF→Markdown の主要 OSS は MarkItDown（軽量・モデル不要）、Docling（レイアウトモデル + VLM、初回 2GB DL）、Marker（Surya OCR、GPU 前提）の 3 強。
- SDD は「要件 → 設計 → タスク → 実装」の 4 フェーズが事実上の標準（GitHub Spec Kit / Kiro）。Kiro は要件を EARS 記法で書く方式。
- 方針決定: 依頼どおり**軽量ローカル優先**。Office 形式は ZIP+XML を直接読み、外部依存ゼロ。高精度エンジンは将来プラグインとして差せる構造にした。

## 取り込んだフィードバック
- 初回のためなし（方針は対話で確認：軽量ローカル優先 / CLI 先行・GUI 後追い / LLM はオプトインのみ / ループは Routine）

## やったこと

| タスク | 内容 | 変更ファイル |
|---|---|---|
| T-00 | 中間表現（IR）とレンダラ | `src/mdconv/model.py`, `renderer.py` |
| T-00 | Word / Excel / PowerPoint / PDF のコンバータ | `src/mdconv/converters/*.py` |
| T-00 | 形式判定（拡張子 + 中身のシグネチャ） | `src/mdconv/registry.py` |
| T-00 | 公開 API と CLI | `src/mdconv/api.py`, `cli.py` |
| T-00 | テスト 72 件 + 手書き OOXML のフィクスチャ | `tests/` |
| — | 仕様書一式とループ手順書 | `docs/` |

## 👀 見てほしいところ

1. **[05-変換ルール](../../specs/05-conversion-rules.md)** — 「元の資料がどう Markdown になるか」の一覧。
   ここが実際の資料と合っているかが最も重要。違和感があれば指摘してほしい。
2. **[02-要件](../../specs/02-requirements.md)** — 「まだできないこと」（📋 の行）が期待とずれていないか。
3. **[04-タスク](../../specs/04-tasks.md)** — 次にやる順番。上から着手する。

## ❓ 判断を仰ぎたいこと

1. **Excel の 1 行目を必ずヘッダ扱いしている**（`| 月 | 金額 |` の行）。
   ヘッダのないデータ表では 1 行目が見出しに化ける。実資料はどちらが多いか？
2. **PDF の表が現状は崩れる**。優先度を上げるべきか、それとも Office 形式の精度を先に上げるべきか。
3. **画像は既定で出力せず警告のみ**にしている。既定で `assets/` に書き出す方が自然か？
4. Word の「表題(Title)」と「見出し 1」がどちらも `#` になるため、`#` が 2 つ並ぶ文書ができる。
   表題を YAML フロントマターだけに送るか、見出し全体を 1 段下げるか、現状のままか。（T-15）

## 運用の変更（初回 PR マージ後の追記）

PR #1 がマージされ、ブランチが削除された。これに伴い運用を次のとおり変更した。

| 項目 | 変更前 | 変更後 |
|---|---|---|
| push 先 | `claude/…` ブランチ → PR → 人間がマージ | **`main` に直接**（事前承認なし） |
| 品質の担保 | 人間の PR レビュー | **AI による独立レビュー**（LOOP.md ⑦。省略禁止） |
| 人間のレビュー | マージ前 | 事後（journal と INBOX で往復） |

`LOOP.md` に ⑦ AI レビュー工程を追加し、journal テンプレートに「レビュー結果」欄を設けた。

## 積み残し
なし（初回のためタスクは新規作成のみ）

## 状態
テスト: 72 件すべて成功 ／ lint: 問題なし

---

# 2026-08-15（追加ラウンド：保留していた 3 件の仕様決定）

「判断を仰ぎたいこと」に挙げた 4 件すべてに回答をもらい、実装まで完了した。

## 決まったこと

| 論点 | 決定 | 実装 |
|---|---|---|
| Excel の見出し行 | **自動判定**（1 行目固定をやめる） | T-12 / FR-214 |
| Word の表題と見出し 1 が両方 `#` | **表題は本文に出さない**（文書のタイトルとして保持） | T-15 / FR-215 |
| 画像の既定 | **既定で書き出す**（`--no-images` で無効） | T-16 / FR-302 |
| 次の優先度 | **実ファイルでの回帰テスト（T-01）** を最優先 | 次回ループで着手 |

## レビュー結果

`/code-review`（high）を実行。**5 件の指摘、すべて対応済み。**

| 指摘 | 重さ | 対応 |
|---|---|---|
| 一括変換で別文書の `image1.png` が互いを上書きし、本文の参照と中身がずれる | 重大 | 画像を `assets/<文書名>/` に分けた。回帰テスト `test_images_of_different_documents_do_not_collide` を追加 |
| docProps にタイトルがあると、本文の表題が消える（警告の「--front-matter で残せます」が嘘になる） | 重大 | 本文の表題を優先するようにした。2 つ目以降の表題は段落として残す |
| `--max-rows` で打ち切った後に見出し行を判定していたため、判定が変わることがある | 中 | 判定を打ち切り前の表全体で行うようにした |
| `test_subtitle_becomes_a_paragraph` が空振り（フィクスチャに Subtitle スタイルが無く、実装を消してもテストが通る） | 中 | フィクスチャに Subtitle スタイルを追加 |
| `04-tasks.md` に廃止した `--extract-images` の記述が残っている | 軽 | 記述を修正 |

**レビューが機能した。** 上位 2 件はテストが通っていたのに壊れていた種類の欠陥で、
実装した文脈のままでは見つけられなかった。⑦の工程は今後も省略しない。

## 👀 見てほしいところ

1. [05-変換ルール](../../specs/05-conversion-rules.md#見出し行の自動判定) の **Excel 見出し行の判定表**。
   「全部文字列の表は見出しなし扱い」が実資料の感覚と合っているか。
2. 画像の置き場所が `assets/<文書名>/image1.png` になった点。フォルダが深くなるのが嫌なら戻せる。
3. Word の表題は既定では**どこにも本文出力されない**（警告のみ）。
   `--front-matter` を常時有効にすべきかは要検討。

## ❓ 判断を仰ぎたいこと
なし（前回の 4 件はすべて解決した）

## 状態
テスト: 85 件すべて成功 ／ lint: 問題なし ／ AI レビュー: 指摘 5 件すべて対応済み
